@model IEnumerable<TimetableSys_T17.Room>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Create Room</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<table class="table">
    <tr>
        <th>
            Room Code
        </th>
        <th>
            Building Name
        </th>
        <th>
            Capacity
        </th>
        <th>
            Lab
        </th>
        <th></th>
        <th></th>
        <th></th>
    </tr>

@foreach (var item in @ViewBag.createdRooms) {
    <tr>
        <td>
            @item.roomCode
        </td>
        <td>
           @item.Building.buildingName
        </td>
        <td>
           @item.capacity
        </td>
        @if (item.lab == 1)
        {
            <td>Yes</td>     
        }
        else
        {
            <td>No</td>
        }
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.roomID })
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id=item.roomID })
        </td>
        <td>
            @Html.ActionLink("Delete", "Delete", new { id=item.roomID })
        </td>
    </tr>
}

</table>